import { Routes } from '@angular/router';
import { LinearCheckerComponent } from './components/linear-checker/linear-checker.component';

export const routes: Routes = [
    {
        path:'',component:LinearCheckerComponent
    }
];
